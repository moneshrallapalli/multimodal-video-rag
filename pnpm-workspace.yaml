packages:
  - "apps/*"
  - "packages/*"

# These packages are reviewed transitive install scripts needed by pnpm 11's
# build approval policy in CI/Vercel.
onlyBuiltDependencies:
  - msw
  - sharp
  - unrs-resolver
