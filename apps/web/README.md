# web

Next.js frontend: public search (read-only over the demo library), the
admin ingestion console, and the interactive evaluation dashboard. Shares one
shadcn/ui design system across all three surfaces.

Initialized with `create-next-app` in Phase 1.
